/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.builder.io'],
    }
};

// module.exports = {
//   images: {
//     domains: ['cdn.builder.io'],
//   },
// };
export default nextConfig;
